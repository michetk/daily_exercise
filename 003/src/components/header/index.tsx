import { HeaderContainer, HeaderTitle } from './styled'

const Header = () => {
  return (
    <HeaderContainer>
      <HeaderTitle>Header</HeaderTitle>
    </HeaderContainer>
  )
}

export default Header
